# Static snapshot (unused in production)

The live bulletin is `../site` (FastAPI + `/admin`).

`index.html` here is only a checked-in snapshot of the default page, for
reading the fixture without running Python. Do not deploy this folder.
